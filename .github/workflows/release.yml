name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (for example v0.1.0-rc.1)"
        required: true
        type: string
      prerelease:
        description: "Mark release as prerelease"
        required: false
        default: true
        type: boolean

jobs:
  audit:
    name: Audit
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@779680da715d629ac1d338a641029a2f4372abb5 # v2.8.2

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Audit lockfile
        run: cargo audit --deny warnings

  build:
    name: Build (${{ matrix.target }})
    needs: audit
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: docwarp-linux-x86_64
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: docwarp-macos-x86_64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: docwarp-macos-aarch64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: docwarp-windows-x86_64.exe

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: Swatinem/rust-cache@779680da715d629ac1d338a641029a2f4372abb5 # v2.8.2

      - name: Build release
        run: cargo build --release --target ${{ matrix.target }} -p docwarp-cli

      - name: Prepare artifact (unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/docwarp dist/${{ matrix.artifact }}

      - name: Prepare artifact (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist
          Copy-Item "target/${{ matrix.target }}/release/docwarp.exe" "dist/${{ matrix.artifact }}"

      - name: Upload artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ${{ matrix.artifact }}
          path: dist/${{ matrix.artifact }}

  publish:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      prerelease: ${{ steps.meta.outputs.prerelease }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Resolve release metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            tag="${{ inputs.tag }}"
            prerelease="${{ inputs.prerelease }}"
          else
            tag="${GITHUB_REF_NAME}"
            if [[ "${tag}" == *-* ]]; then
              prerelease="true"
            else
              prerelease="false"
            fi
          fi

          if [[ "${tag}" != v* ]]; then
            echo "tag must start with 'v': ${tag}" >&2
            exit 1
          fi

          version="${tag#v}"
          echo "tag=${tag}" >> "${GITHUB_OUTPUT}"
          echo "version=${version}" >> "${GITHUB_OUTPUT}"
          echo "prerelease=${prerelease}" >> "${GITHUB_OUTPUT}"

      - name: Download build artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: docwarp-*
          merge-multiple: true
          path: dist

      - name: Generate checksums and Homebrew formula
        shell: bash
        run: |
          set -euo pipefail
          ls -la dist

          sha256sum dist/docwarp-linux-x86_64 > dist/checksums.txt
          sha256sum dist/docwarp-macos-x86_64 >> dist/checksums.txt
          sha256sum dist/docwarp-macos-aarch64 >> dist/checksums.txt
          sha256sum dist/docwarp-windows-x86_64.exe >> dist/checksums.txt

          linux_sha="$(awk '/docwarp-linux-x86_64$/ { print $1 }' dist/checksums.txt)"
          macos_x86_sha="$(awk '/docwarp-macos-x86_64$/ { print $1 }' dist/checksums.txt)"
          macos_arm_sha="$(awk '/docwarp-macos-aarch64$/ { print $1 }' dist/checksums.txt)"

          owner_repo="${GITHUB_REPOSITORY}"
          owner="${owner_repo%%/*}"
          repo="${owner_repo##*/}"

          scripts/generate_homebrew_formula.sh \
            "${owner}" \
            "${repo}" \
            "${{ steps.meta.outputs.version }}" \
            "${macos_arm_sha}" \
            "${macos_x86_sha}" \
            "${linux_sha}" \
            "dist/docwarp.rb"

      - name: Create release notes body
        shell: bash
        run: |
          {
            echo "## Release artifacts"
            echo
            echo "- docwarp-linux-x86_64"
            echo "- docwarp-macos-x86_64"
            echo "- docwarp-macos-aarch64"
            echo "- docwarp-windows-x86_64.exe"
            echo "- checksums.txt"
            echo "- docwarp.rb (Homebrew formula with release checksums)"
            echo
            echo "## Verification"
            echo
            echo "Use checksums.txt to verify downloaded binaries."
            echo
            echo "## Changelog"
            echo
            echo "See CHANGELOG.md in this tag."
          } > dist/release-notes.md

      - name: Publish release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2.5.0
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          prerelease: ${{ steps.meta.outputs.prerelease }}
          body_path: dist/release-notes.md
          files: |
            dist/docwarp-linux-x86_64
            dist/docwarp-macos-x86_64
            dist/docwarp-macos-aarch64
            dist/docwarp-windows-x86_64.exe
            dist/checksums.txt
            dist/docwarp.rb

  smoke:
    name: Smoke Published Binary (${{ matrix.os }})
    needs: publish
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
          - os: macos-latest
            platform: macos
          - os: windows-latest
            platform: windows
    env:
      RELEASE_TAG: ${{ needs.publish.outputs.tag }}
    steps:
      - name: Download and run smoke test (unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail

          case "${{ matrix.platform }}" in
            linux)
              asset="docwarp-linux-x86_64"
              ;;
            macos)
              if [[ "${RUNNER_ARCH}" == "ARM64" ]]; then
                asset="docwarp-macos-aarch64"
              else
                asset="docwarp-macos-x86_64"
              fi
              ;;
            *)
              echo "unexpected platform: ${{ matrix.platform }}" >&2
              exit 1
              ;;
          esac

          url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/releases/download/${RELEASE_TAG}/${asset}"

          for attempt in {1..10}; do
            if curl -fL \
              -H "Authorization: Bearer ${GITHUB_TOKEN}" \
              -H "Accept: application/octet-stream" \
              "${url}" \
              -o "docwarp"; then
              break
            fi
            if [[ "${attempt}" == "10" ]]; then
              echo "failed to download release asset after retries: ${url}" >&2
              exit 1
            fi
            sleep 3
          done

          chmod +x "docwarp"
          ./docwarp --help
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Download and run smoke test (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $url = "${env:GITHUB_SERVER_URL}/${env:GITHUB_REPOSITORY}/releases/download/${env:RELEASE_TAG}/docwarp-windows-x86_64.exe"
          $headers = @{
            Authorization = "Bearer $env:GITHUB_TOKEN"
            Accept = "application/octet-stream"
          }

          $downloaded = $false
          for ($attempt = 1; $attempt -le 10; $attempt++) {
            try {
              Invoke-WebRequest -Uri $url -Headers $headers -OutFile "docwarp.exe"
              $downloaded = $true
              break
            } catch {
              if ($attempt -eq 10) { throw }
              Start-Sleep -Seconds 3
            }
          }

          if (-not $downloaded) {
            throw "Failed to download asset from $url"
          }

          .\docwarp.exe --help
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
