# Docwarp Sample Document

This is a sample Markdown file for testing DOCX conversion.

## Overview

- Convert Markdown to DOCX with `docwarp md2docx`
- Convert DOCX back to Markdown with `docwarp docx2md`
- Use `--strict` to fail on warnings

## Notes

This paragraph includes **bold text**, *italic text*, and `inline code`.

### Next Steps

1. Run the conversion command.
2. Open the generated `.docx` file in Word or another compatible editor.
3. Confirm formatting looks correct.
